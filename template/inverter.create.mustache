// inverter layer '{{&name}}'
{	
	current->{{&name}} = inverter_layer_create( (GRect){ .origin = { {{top}}, {{left}} }, .size = { {{width}}, {{height}} } } );
	Layer *layer = bitmap_layer_get_layer( current->{{&name}} );
	{{#hidden}}
	layer_set_hidden( layer, true );
	{{/hidden}}
    layer_add_child( parent, layer );
}